# Food Fate 小程序设计文档

## 项目目录结构

```
food-fate/
├── pages/                    # 页面文件夹
│   ├── index/               # 首页
│   └── logs/                # 日志页面
├── utils/                   # 工具函数文件夹
│   └── util.js             # 通用工具函数
├── app.js                   # 小程序入口文件
├── app.json                 # 小程序全局配置文件
├── app.wxss                 # 小程序全局样式文件
├── project.config.json      # 项目配置文件
├── project.private.config.json # 项目私有配置文件
├── sitemap.json            # 小程序搜索配置文件
└── .eslintrc.js           # ESLint 配置文件
```

## 技术要点

### 1. 开发框架
- 使用微信小程序原生框架进行开发
- 采用 MVVM 架构模式
- 使用 ESLint 进行代码规范检查

### 2. 项目配置
- 通过 `project.config.json` 管理项目配置
- 使用 `sitemap.json` 配置小程序搜索相关设置
- 全局样式通过 `app.wxss` 统一管理

### 3. 目录组织
- 页面文件统一放在 `pages` 目录下
- 工具函数统一放在 `utils` 目录下
- 每个页面包含 `.js`、`.wxml`、`.wxss`、`.json` 四个文件

### 4. 开发规范
- 使用 ESLint 进行代码规范检查
- 遵循微信小程序开发规范
- 采用模块化的开发方式

### 5. 工具函数
- 通用工具函数统一放在 `utils/util.js` 中
- 包含常用的数据处理、格式化等功能

## 注意事项
1. 开发时请遵循微信小程序的开发规范
2. 保持代码结构清晰，遵循模块化开发原则
3. 注意性能优化，合理使用组件化开发
4. 保持代码风格统一，遵循 ESLint 规范

## 后续更新
本文档将根据项目开发进度持续更新，包括但不限于：
- 新增功能模块说明
- API 接口文档
- 组件使用说明
- 性能优化建议 